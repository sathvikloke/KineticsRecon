# KineticsRecon requirements
# Install with: pip install -r requirements.txt

torch>=2.0.0
torchvision>=0.15.0
h5py>=3.7.0
nibabel>=5.0.0
numpy>=1.24.0
scipy>=1.10.0
scikit-learn>=1.2.0     # for bootstrap CI in ablation study
matplotlib>=3.7.0       # for figures
pandas>=2.0.0           # for CSV/Excel handling
openpyxl>=3.1.0        # for reading clinical_and_imaging_info.xlsx
tqdm>=4.65.0
pyyaml>=6.0

# Optional but recommended
lightning>=2.0.0        # PyTorch Lightning (alternative training loop)
wandb>=0.15.0           # experiment tracking
